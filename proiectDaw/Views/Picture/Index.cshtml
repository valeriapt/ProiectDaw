@using proiectDaw.Models
@model IEnumerable<proiectDaw.Models.Pictures>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    @using (Html.BeginForm("Index", "Picture", FormMethod.Post))
    {
        <p>ghfds</p>
        foreach (Pictures image in Model)
        {
            <p> @image.Image.Length</p>
            String source = "data:image/jpg;base64," + @Convert.ToBase64String(@image.Image, 0, @image.Image.Length);

              <hr />
                <img alt="askf" src=@source  style="height:100px;width:100px;" />
            
        }
    }

</body>
</html>